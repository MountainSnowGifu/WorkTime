<UserControl x:Class="WorkTime.Views.TestView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"             
             prism:ViewModelLocator.AutoWireViewModel="True">
    <Grid>
        <StackPanel>
            <Button Content="Update" Command="{Binding TestCommand}"/>
            <Button Content="Update" Command="{Binding TestCommand2}"/>
            <Button Content="test3" Command="{Binding TestCommand3}"/>
            <Button Content="test4" Command="{Binding TestCommand4}"/>
            <Button Content="test5" Command="{Binding TestCommand5}"/>
            <Button Content="test6" Command="{Binding TestCommand6}"/>
        </StackPanel>
    </Grid>
</UserControl>
